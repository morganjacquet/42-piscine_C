find . | wc -l | sed 's/ //g'
#ls -l * permet de recuperer tout les fichier du repertoire et sous repertoire
#grep permet de compter tous les fichier avec -c on ne presise rien pour prendre tout les fichier
